<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Visualizador de Planilla</title>
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        body {
            background-color: #f5f7fa;
            color: #333;
            line-height: 1.6;
            padding: 20px;
        }
        
        .container {
            max-width: 1400px;
            margin: 0 auto;
            background-color: white;
            border-radius: 10px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
            padding: 25px;
        }
        
        header {
            text-align: center;
            margin-bottom: 30px;
            border-bottom: 1px solid #eaeaea;
            padding-bottom: 20px;
        }
        
        h1 {
            color: #2c3e50;
            margin-bottom: 10px;
        }
        
        .description {
            color: #7f8c8d;
            font-size: 1.1rem;
            max-width: 800px;
            margin: 0 auto;
        }
        
        .config-section {
            background-color: #f8f9fa;
            border-radius: 8px;
            padding: 20px;
            margin-bottom: 30px;
            border-left: 4px solid #3498db;
        }
        
        .config-title {
            font-size: 1.2rem;
            color: #2c3e50;
            margin-bottom: 15px;
            display: flex;
            align-items: center;
        }
        
        .config-title i {
            margin-right: 10px;
            color: #3498db;
        }
        
        .url-info {
            background-color: #e8f4fc;
            border-radius: 5px;
            padding: 15px;
            margin-bottom: 20px;
            border-left: 3px solid #3498db;
        }
        
        .url-info code {
            background-color: #2c3e50;
            color: white;
            padding: 5px 10px;
            border-radius: 3px;
            font-family: monospace;
            word-break: break-all;
            display: inline-block;
            margin-top: 5px;
        }
        
        .btn {
            padding: 12px 25px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1rem;
            font-weight: 600;
            transition: all 0.3s;
        }
        
        .btn-primary {
            background-color: #3498db;
            color: white;
        }
        
        .btn-primary:hover {
            background-color: #2980b9;
        }
        
        .btn-success {
            background-color: #2ecc71;
            color: white;
        }
        
        .btn-success:hover {
            background-color: #27ae60;
        }
        
        .btn-print {
            background-color: #9b59b6;
            color: white;
        }
        
        .btn-print:hover {
            background-color: #8e44ad;
        }
        
        .button-group {
            display: flex;
            gap: 10px;
            flex-wrap: wrap;
            margin-top: 20px;
        }
        
        .filters-section {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }
        
        .filter-card {
            background-color: white;
            border-radius: 8px;
            padding: 15px;
            border: 1px solid #eaeaea;
            box-shadow: 0 3px 5px rgba(0, 0, 0, 0.03);
        }
        
        .filter-card h3 {
            font-size: 1rem;
            color: #3498db;
            margin-bottom: 10px;
            padding-bottom: 8px;
            border-bottom: 1px solid #f0f0f0;
        }
        
        select, input[type="text"] {
            width: 100%;
            padding: 10px 12px;
            border: 1px solid #ddd;
            border-radius: 5px;
            font-size: 1rem;
            transition: border 0.3s;
        }
        
        select:focus, input[type="text"]:focus {
            border-color: #3498db;
            outline: none;
        }
        
        .data-section {
            margin-top: 30px;
        }
        
        .data-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            flex-wrap: wrap;
            gap: 15px;
        }
        
        .results-info {
            font-weight: 600;
            color: #2c3e50;
        }
        
        .table-container {
            overflow-x: auto;
            border-radius: 8px;
            border: 1px solid #eaeaea;
            margin-bottom: 30px;
        }
        
        table {
            width: 100%;
            border-collapse: collapse;
            min-width: 1000px;
        }
        
        thead {
            background-color: #2c3e50;
            color: white;
        }
        
        th {
            padding: 15px;
            text-align: left;
            font-weight: 600;
        }
        
        tbody tr {
            border-bottom: 1px solid #eaeaea;
            transition: background-color 0.2s;
        }
        
        tbody tr:nth-child(even) {
            background-color: #f9f9f9;
        }
        
        tbody tr:hover {
            background-color: #f1f8ff;
        }
        
        td {
            padding: 12px 15px;
        }
        
        .tag {
            display: inline-block;
            background-color: #e1f5fe;
            color: #0288d1;
            padding: 4px 10px;
            border-radius: 20px;
            font-size: 0.85rem;
            margin: 2px;
        }
        
        .no-data {
            text-align: center;
            padding: 40px;
            color: #7f8c8d;
            font-style: italic;
        }
        
        .loading {
            text-align: center;
            padding: 30px;
            color: #3498db;
            font-weight: 600;
        }
        
        .error {
            text-align: center;
            padding: 30px;
            color: #e74c3c;
            font-weight: 600;
            background-color: #ffeaea;
            border-radius: 8px;
            margin-top: 20px;
        }
        
        @media print {
            .no-print {
                display: none !important;
            }
            
            .container {
                box-shadow: none;
            }
            
            .table-container {
                overflow: visible;
                border: none;
            }
            
            tbody tr {
                page-break-inside: avoid;
            }
        }
        
        @media (max-width: 768px) {
            .button-group {
                flex-direction: column;
            }
            
            .btn {
                width: 100%;
            }
            
            .filters-section {
                grid-template-columns: 1fr;
            }
        }
    </style>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
    <div class="container">
        <header>
            <h1><i class="fas fa-table"></i> Visualizador de Planilla</h1>
            <p class="description">Visualiza y filtra datos de planillas desde Google Sheets. Filtra por zona, subzona, categoría y más. Imprime los resultados filtrados.</p>
        </header>
        
        <section class="config-section no-print">
            <h2 class="config-title"><i class="fas fa-database"></i> Fuente de Datos</h2>
            <div class="url-info">
                <p><strong>Google Sheets conectado:</strong></p>
                <code id="sheet-url-display">Cargando información...</code>
                <p style="margin-top: 10px; font-size: 0.9rem;">Los datos se cargan automáticamente desde la URL configurada en el código.</p>
            </div>
            <button id="load-data" class="btn btn-primary">
                <i class="fas fa-sync-alt"></i> Actualizar Datos
            </button>
            <button id="change-sheet" class="btn btn-primary" style="margin-left: 10px;">
                <i class="fas fa-exchange-alt"></i> Cambiar Hoja
            </button>
        </section>
        
        <section class="filters-section no-print">
            <div class="filter-card">
                <h3><i class="fas fa-filter"></i> Filtro por Zona (G)</h3>
                <select id="filter-zona">
                    <option value="">Todas las zonas</option>
                </select>
            </div>
            
            <div class="filter-card">
                <h3><i class="fas fa-filter"></i> Filtro por Subzona (H)</h3>
                <select id="filter-subzona">
                    <option value="">Todas las subzonas</option>
                </select>
            </div>
            
            <div class="filter-card">
                <h3><i class="fas fa-filter"></i> Filtro por Categoría (M)</h3>
                <select id="filter-categoria">
                    <option value="">Todas las categorías</option>
                </select>
            </div>
            
            <div class="filter-card">
                <h3><i class="fas fa-filter"></i> Filtro por Nombre (I)</h3>
                <input type="text" id="filter-nombre" placeholder="Buscar por nombre...">
            </div>
        </section>
        
        <section class="data-section">
            <div class="data-header">
                <div class="results-info">
                    <span id="result-count">0</span> registros encontrados
                </div>
                <div class="button-group no-print">
                    <button id="apply-filters" class="btn btn-success">
                        <i class="fas fa-filter"></i> Aplicar Filtros
                    </button>
                    <button id="reset-filters" class="btn btn-primary">
                        <i class="fas fa-redo"></i> Restablecer Filtros
                    </button>
                    <button id="print-data" class="btn btn-print">
                        <i class="fas fa-print"></i> Imprimir Listado
                    </button>
                </div>
            </div>
            
            <div id="data-container">
                <div class="loading">
                    <i class="fas fa-spinner fa-spin"></i> Cargando datos desde Google Sheets...
                </div>
            </div>
        </section>
    </div>

    <script>
        // ============================================
        // CONFIGURACIÓN - EDITAR ESTA SECCIÓN
        // ============================================
        
        // URL de tu Google Sheets (REEMPLAZAR CON TU URL)
        const GOOGLE_SHEETS_URL = "https://docs.google.com/spreadsheets/d/e/2PACX-1vTiCM3t58H6ASsLOPRVaeCpE8OWbdK6Gyf62PA54QHG0ZniqGiY4jaRdPEgzs05-I37xcd_GLdwZTM7/pub?gid=1874394039&single=true&output=csv";
        
        // Nombre de la hoja (dejar vacío para usar la primera hoja)
        const SHEET_NAME = ""; // Ejemplo: "Hoja1" o "Datos"
        
        // ============================================
        // NO EDITAR A PARTIR DE AQUÍ
        // ============================================
        
        // Variables globales
        let allData = [];
        let filteredData = [];
        let filterZona = document.getElementById('filter-zona');
        let filterSubzona = document.getElementById('filter-subzona');
        let filterCategoria = document.getElementById('filter-categoria');
        let filterNombre = document.getElementById('filter-nombre');
        let loadDataBtn = document.getElementById('load-data');
        let applyFiltersBtn = document.getElementById('apply-filters');
        let resetFiltersBtn = document.getElementById('reset-filters');
        let printDataBtn = document.getElementById('print-data');
        let dataContainer = document.getElementById('data-container');
        let resultCount = document.getElementById('result-count');
        let sheetUrlDisplay = document.getElementById('sheet-url-display');
        let changeSheetBtn = document.getElementById('change-sheet');
        
        // Mostrar URL actual
        sheetUrlDisplay.textContent = GOOGLE_SHEETS_URL;
        
        // Cargar datos desde Google Sheets
        async function loadDataFromSheet() {
            // Mostrar estado de carga
            dataContainer.innerHTML = '<div class="loading"><i class="fas fa-spinner fa-spin"></i> Cargando datos desde Google Sheets...</div>';
            
            try {
                // Usar la URL configurada
                let exportUrl = GOOGLE_SHEETS_URL;
                
                // Si se especificó un nombre de hoja y la URL no lo tiene, intentar agregarlo
                if (SHEET_NAME && !exportUrl.includes('sheet=')) {
                    if (exportUrl.includes('?')) {
                        exportUrl += `&sheet=${encodeURIComponent(SHEET_NAME)}`;
                    } else {
                        exportUrl += `?sheet=${encodeURIComponent(SHEET_NAME)}`;
                    }
                }
                
                // Cargar datos CSV
                const response = await fetch(exportUrl);
                if (!response.ok) {
                    throw new Error(`Error HTTP: ${response.status}`);
                }
                const csvText = await response.text();
                
                // Parsear CSV a JSON
                allData = parseCSV(csvText);
                
                // Mostrar datos
                renderData(allData);
                populateFilterOptions(allData);
                
                // Actualizar contador
                updateResultCount(allData.length);
                
            } catch (error) {
                console.error('Error cargando datos:', error);
                showError(`Error al cargar datos: ${error.message}. Verifica que la hoja esté compartida públicamente y que la URL sea correcta.`);
            }
        }
        
        // Parsear CSV a JSON
        function parseCSV(csvText) {
            const rows = csvText.split('\n')
                .filter(row => row.trim() !== '') // Filtrar filas vacías
                .map(row => {
                    // Manejo de comillas y comas dentro de campos
                    const result = [];
                    let inQuotes = false;
                    let currentField = '';
                    
                    for (let i = 0; i < row.length; i++) {
                        const char = row[i];
                        const nextChar = row[i + 1];
                        
                        if (char === '"' && !inQuotes) {
                            inQuotes = true;
                        } else if (char === '"' && inQuotes && nextChar === '"') {
                            currentField += '"';
                            i++; // Saltar la siguiente comilla
                        } else if (char === '"' && inQuotes) {
                            inQuotes = false;
                        } else if (char === ',' && !inQuotes) {
                            result.push(currentField.trim());
                            currentField = '';
                        } else {
                            currentField += char;
                        }
                    }
                    
                    result.push(currentField.trim());
                    return result;
                });
            
            if (rows.length < 2) return [];
            
            // Obtener encabezados
            const headers = rows[0];
            
            // Crear objetos para cada fila de datos
            const data = [];
            for (let i = 1; i < rows.length; i++) {
                const row = rows[i];
                
                // Verificar que tenga suficientes columnas
                if (row.length >= 7) { // Al menos 7 columnas (de G a M)
                    const rowData = {};
                    
                    // Asignar datos según las columnas de la planilla
                    // Ajustar índices según la estructura real de tu CSV
                    rowData.zona = row[0] || ''; // Columna G
                    rowData.subzona = row[1] || ''; // Columna H
                    rowData.nombre = row[2] || ''; // Columna I
                    rowData.domicilio = row[3] || ''; // Columna J
                    rowData.dni = row[4] || ''; // Columna K
                    rowData.edad = row[5] || ''; // Columna L
                    rowData.categoria = row[6] || ''; // Columna M
                    
                    // Tags (columnas N, O, P, Q)
                    rowData.tags = [];
                    for (let j = 7; j < Math.min(11, row.length); j++) {
                        if (row[j]) {
                            rowData.tags.push(row[j]);
                        }
                    }
                    
                    data.push(rowData);
                }
            }
            
            return data;
        }
        
        // Mostrar datos en tabla
        function renderData(data) {
            if (!data || data.length === 0) {
                dataContainer.innerHTML = '<div class="no-data"><i class="fas fa-exclamation-circle"></i> No hay datos para mostrar</div>';
                return;
            }
            
            let tableHTML = `
                <div class="table-container">
                    <table>
                        <thead>
                            <tr>
                                <th>Zona</th>
                                <th>Subzona</th>
                                <th>Nombre</th>
                                <th>Domicilio</th>
                                <th>DNI</th>
                                <th>Edad</th>
                                <th>Categoría</th>
                                <th>Tags</th>
                            </tr>
                        </thead>
                        <tbody>
            `;
            
            data.forEach(item => {
                tableHTML += `
                    <tr>
                        <td>${escapeHtml(item.zona)}</td>
                        <td>${escapeHtml(item.subzona)}</td>
                        <td>${escapeHtml(item.nombre)}</td>
                        <td>${escapeHtml(item.domicilio)}</td>
                        <td>${escapeHtml(item.dni)}</td>
                        <td>${escapeHtml(item.edad)}</td>
                        <td>${escapeHtml(item.categoria)}</td>
                        <td>${item.tags.map(tag => `<span class="tag">${escapeHtml(tag)}</span>`).join(' ')}</td>
                    </tr>
                `;
            });
            
            tableHTML += `
                        </tbody>
                    </table>
                </div>
            `;
            
            dataContainer.innerHTML = tableHTML;
        }
        
        // Escapar HTML para seguridad
        function escapeHtml(text) {
            const div = document.createElement('div');
            div.textContent = text;
            return div.innerHTML;
        }
        
        // Llenar opciones de filtro
        function populateFilterOptions(data) {
            // Limpiar opciones actuales
            filterZona.innerHTML = '<option value="">Todas las zonas</option>';
            filterSubzona.innerHTML = '<option value="">Todas las subzonas</option>';
            filterCategoria.innerHTML = '<option value="">Todas las categorías</option>';
            
            // Obtener valores únicos para cada filtro
            const zonas = [...new Set(data.map(item => item.zona).filter(Boolean))].sort();
            const subzonas = [...new Set(data.map(item => item.subzona).filter(Boolean))].sort();
            const categorias = [...new Set(data.map(item => item.categoria).filter(Boolean))].sort();
            
            // Agregar opciones a los filtros
            zonas.forEach(zona => {
                filterZona.innerHTML += `<option value="${escapeHtml(zona)}">${escapeHtml(zona)}</option>`;
            });
            
            subzonas.forEach(subzona => {
                filterSubzona.innerHTML += `<option value="${escapeHtml(subzona)}">${escapeHtml(subzona)}</option>`;
            });
            
            categorias.forEach(categoria => {
                filterCategoria.innerHTML += `<option value="${escapeHtml(categoria)}">${escapeHtml(categoria)}</option>`;
            });
        }
        
        // Aplicar filtros
        function applyFilters() {
            const zonaValue = filterZona.value;
            const subzonaValue = filterSubzona.value;
            const categoriaValue = filterCategoria.value;
            const nombreValue = filterNombre.value.toLowerCase();
            
            filteredData = allData.filter(item => {
                // Filtro por zona
                if (zonaValue && item.zona !== zonaValue) return false;
                
                // Filtro por subzona
                if (subzonaValue && item.subzona !== subzonaValue) return false;
                
                // Filtro por categoría
                if (categoriaValue && item.categoria !== categoriaValue) return false;
                
                // Filtro por nombre (búsqueda parcial)
                if (nombreValue && !item.nombre.toLowerCase().includes(nombreValue)) return false;
                
                return true;
            });
            
            renderData(filteredData);
            updateResultCount(filteredData.length);
        }
        
        // Restablecer filtros
        function resetFilters() {
            filterZona.value = '';
            filterSubzona.value = '';
            filterCategoria.value = '';
            filterNombre.value = '';
            
            renderData(allData);
            updateResultCount(allData.length);
        }
        
        // Imprimir datos
        function printData() {
            window.print();
        }
        
        // Actualizar contador de resultados
        function updateResultCount(count) {
            resultCount.textContent = count;
        }
        
        // Mostrar error
        function showError(message) {
            dataContainer.innerHTML = `<div class="error"><i class="fas fa-exclamation-circle"></i> ${message}</div>`;
        }
        
        // Función para cambiar la URL de la hoja (solo para desarrollo)
        function changeSheetUrl() {
            const newUrl = prompt("Ingresa la nueva URL de Google Sheets:", GOOGLE_SHEETS_URL);
            if (newUrl) {
                // Esta función solo funciona si editas el código
                // En producción, deberías editar la constante GOOGLE_SHEETS_URL
                alert("Para cambiar la URL permanentemente, edita la constante GOOGLE_SHEETS_URL en el código.\n\nURL ingresada: " + newUrl);
                sheetUrlDisplay.textContent = newUrl;
                
                // Recargar con la nueva URL (solo para esta sesión)
                setTimeout(() => {
                    alert("Recuerda que para un cambio permanente debes editar el código. Recargando datos con la nueva URL...");
                    // Nota: Esto no cambiará realmente la URL sin recargar la página
                    location.reload();
                }, 1000);
            }
        }
        
        // Event Listeners
        loadDataBtn.addEventListener('click', loadDataFromSheet);
        applyFiltersBtn.addEventListener('click', applyFilters);
        resetFiltersBtn.addEventListener('click', resetFilters);
        printDataBtn.addEventListener('click', printData);
        changeSheetBtn.addEventListener('click', changeSheetUrl);
        
        // Permitir aplicar filtros con Enter en el campo de nombre
        filterNombre.addEventListener('keyup', (event) => {
            if (event.key === 'Enter') {
                applyFilters();
            }
        });
        
        // Cargar datos automáticamente al inicio
        window.addEventListener('DOMContentLoaded', loadDataFromSheet);
    </script>
</body>
</html>
